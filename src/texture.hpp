#pragma once
#ifndef TEXTURE_HPP
#define TEXTURE_HPP

/*


class Texture {
public:
    Texture();
    Texture(const string& pathToImage);

    void loadFrom(const string& pathToImage);

private:
    SDL_Texture* texture;

};
*/
#endif // TEXTURE_HPP
